# The registry and --dry-run need only the standard library.
# Extraction (A1–A4) reads word boxes with pdfplumber, which brings pypdfium2
# for crops and page images; the bank is validated with jsonschema.
# Packaging (A9) also needs the qpdf command (brew install qpdf).
pdfplumber>=0.11,<0.12
jsonschema>=4.21
pytest>=8.0
